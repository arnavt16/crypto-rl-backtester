# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Node
node_modules/
dist/
.vite/

# OS
.DS_Store

# Env / secrets -- belt and suspenders, this project has no API keys
# today (the data source needs none), but keep the door shut anyway.
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json

# Model artifacts are small (<200KB) and checked in so the demo works
# without retraining -- see backend/scripts/generate_default_results.py
*.log
