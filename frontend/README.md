# Frontend

React + TypeScript + Vite + Tailwind CSS v4 + Recharts dashboard for the RL
crypto trading bot backtester.

```bash
npm install
npm run dev
```

Requires the backend running on `localhost:8000` (see the [repo root
README](../README.md) for full setup and project documentation).
